# Streamlit & core
streamlit>=1.36,<1.49
huggingface_hub>=0.23,<0.35
safetensors>=0.4.3
Pillow>=10.0
numpy<2.0

# Torch stack (CPU wheels are fine on Streamlit Cloud)
torch==2.2.1
torchvision==0.17.1
timm==1.0.19
pytorch-lightning==2.2.5

# ONNX for YOLO fallback (we use only ONNX for detector)
onnxruntime==1.17.3

# Visualization and image ops (no OpenCV)
matplotlib>=3.8
scikit-image>=0.24

# pytorch-grad-cam (pin to a known commit/tag so uv can resolve)
grad-cam @ git+https://github.com/jacobgil/pytorch-grad-cam
