# Streamlit & core
streamlit>=1.36,<1.49
huggingface_hub>=0.23,<0.35
safetensors>=0.4.3
Pillow>=10.0
numpy<2.0

# Torch stack (CPU build is fine on Streamlit Cloud)
torch==2.2.1
torchvision==0.17.1
timm==1.0.19
pytorch-lightning==2.2.5

# ONNX for YOLO fallback
onnxruntime==1.17.3

# pytorch-grad-cam: pull from GitHub so uv/pip can resolve reliably
grad-cam @ git+https://github.com/jacobgil/pytorch-grad-cam

# OpenCV (headless only)
opencv-python-headless==4.10.0.84
